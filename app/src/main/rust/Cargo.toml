[package]
name = "rust_engine"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
jni = "0.21.1"
# Library image dengan fitur lengkap untuk manipulasi pixel
image = { version = "0.24.7", features = ["jpeg", "png", "webp"] }
